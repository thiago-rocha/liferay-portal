{namespace ddl}

/**
 * Prints the DDL form rules builder cotainer.
 *
 * @param addRuleLabel
 */
{template .rules  autoescape="deprecated-contextual"}
	<div class="form-builder-rules-builder-container">
		<div class="form-builder-rules-builder-rule-list"></div>
		<div class="form-builder-rules-builder-add-rule-container">
			<a class="btn form-builder-rules-builder-add-rule" href="javascript:;">
			<span class="form-builder-rules-builder-add-rule-button-icon">+</span>
			<label class="form-builder-rules-builder-add-rule-button-label">{$addRuleLabel}</label>
			</a>
		</div>
	</div>
	<div class="form-builder-rule-settings-container"></div>
{/template}

/**
 * Prints the DDL form card rule.
 *
 * @param rules
 * @param emptyListText
 * @param kebab
 */
{template .rule_list  autoescape="deprecated-contextual"}
	{foreach $rule in $rules}
		<div class="card card-horizontal card-rule">
			<div class="card-row card-row-padded">
				<div class="card-col-content card-col-gutters">
					<h4>{$rule.type}</h4>
					<p>{$rule.description}</p>
				</div>
				<div class="card-col-field">
					<div class="dropdown">
						<a class="dropdown-toggle icon-monospaced" data-toggle="dropdown" href="#1">
							{$kebab|noAutoescape}
						</a>
						<ul class="dropdown-menu dropdown-menu-right">
							<li class="rule-card-edit" data-card-id={$rule.id}><a href="#1">Edit</a></li>
							<li class="rule-card-delete"  data-card-id={$rule.id}><a href="#1">Delete</a></li>
						</ul>
					</div>
				</div>
			</div>
		</div>
		{ifempty}
			{$emptyListText}
	{/foreach}
{/template}